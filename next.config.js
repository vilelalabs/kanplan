/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,

  i18n: {
    locales: ['en-US', 'pt-BR'],
    defaultLocale: 'en-US',
  }

}


module.exports = nextConfig
